# Copy to .env and fill in for a split-origin deployment (e.g. this frontend
# on Vercel, the backend on Render/Railway). Leave unset for local dev or
# same-origin deployments (Docker Compose's nginx) -- requests then go to
# the same origin the frontend is served from, as before.
VITE_API_URL=
