# Tesseract OCR (offline label reading, recommended - light, works on any
# Python incl. 3.14):  pip install -r requirements-ocr.txt
#      brew install tesseract        # macOS  (Linux: apt-get install tesseract-ocr)
#      pip install pytesseract
#    Metros auto-detects and uses Tesseract when the binary is on PATH.
#
# For PaddleOCR-VL (experimental branch only, heavier, GPU-accelerated), see
# requirements-paddle.txt.

pytesseract>=0.3.13
