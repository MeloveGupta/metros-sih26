# Tesseract OCR (offline label reading, used automatically as the fallback
# when the hosted PaddleOCR API is unconfigured or fails -- or set
# METROS_OCR_ENGINE=tesseract to use it directly):
#      brew install tesseract        # macOS  (Linux: apt-get install tesseract-ocr)
#      pip install -r requirements-ocr.txt
#    Metros auto-detects and uses Tesseract when the binary is on PATH.

pytesseract>=0.3.13
