# PaddleOCR-VL-1.6 (experimental branch only, local testing). GPU-accelerated
# via PaddlePaddle native inference -- see this branch's README section
# "Experimental branch: PaddleOCR-VL (local only)" for setup/limitations.
#
# GPU install (tested against an NVIDIA RTX 4070, CC 8.6, CUDA 13.0 driver;
# the docs only show a cu126 wheel example -- NVIDIA drivers are backward
# compatible, so this is expected to work, not a documented guarantee). For
# a different CUDA version, see the PaddlePaddle install-selector guide:
# https://www.paddlepaddle.org.cn/en/install/quick

--extra-index-url https://www.paddlepaddle.org.cn/packages/stable/cu126/
paddlepaddle-gpu==3.2.1
paddleocr[doc-parser]>=3.6.0

# CPU-only fallback (untested, no documented speed -- expect it to be slow):
# comment out the two lines above and uncomment these two instead.
# --extra-index-url https://www.paddlepaddle.org.cn/packages/stable/cpu/
# paddlepaddle==3.2.1
